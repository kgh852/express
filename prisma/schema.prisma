// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL") // .env 파일에 데이터베이스 URL을 저장합니다.
}

model User {
  id    Int     @id @default(autoincrement())
  username  String @unique
  password String  
}
